// import { create } from 'zustand';
// import * as THREE from 'three';
// import { SoundController } from './SoundController'; // adjust path

// type SoundStore = {
//   soundController: SoundController | null;
//   initSoundController: (scene: THREE.Scene) => void;
// };

// export const useSoundStore = create<SoundStore>((set) => ({
//   soundController: null,

//   initSoundController: (scene: THREE.Scene) => {
//     const controller = new SoundController(scene);
//     set({ soundController: controller });
//   },
// }));
